/* eslint-disable import/extensions */

import { field } from './Validatable';
import { endpoint, authorizer, schema, instance, affects, uses } from './Controllable';

export default { field, endpoint, authorizer, schema, instance, affects, uses };
